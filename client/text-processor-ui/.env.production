# Production Environment Configuration
# API and SignalR calls resolve through the reverse proxy inside Docker.

REACT_APP_API_URL=/api
REACT_APP_SIGNALR_URL=/hubs/processing

# Production settings
GENERATE_SOURCEMAP=false
REACT_APP_ENV=production